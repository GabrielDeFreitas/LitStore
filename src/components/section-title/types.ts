import { ReactNode } from 'react'
import { ElementType } from 'react'

export interface SectionTitleRootProps {
  children: ReactNode
}

export interface SectionTitleIconProps {
  icon: ElementType
}

export interface SectionTitleHeaderProps {
  title: string
}
